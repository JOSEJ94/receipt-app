import { StyleSheet } from "react-native"

export const createStyles = () =>
  StyleSheet.create({
    mainContainer: {
      flex: 1,
    },
    invoicesContainer: {
      backgroundColor: "white",
    },
  })
